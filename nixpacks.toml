[phases.setup]
nixPkgs = ["python310", "opencv", "libGL", "glib"]

[phases.build]
cmds = ["pip install -r server/requirements.txt"]

[start]
cmd = "cd server && gunicorn app:app --host 0.0.0.0 --port $PORT --timeout 30 --workers 1"
